# Make a pretty date

Make a pretty date

## Usage

``` r
pretty_date(x = NULL)
```

## Arguments

- x:

  A `Date`. If `x` is `NULL`, the system date is returned.

## Value

A character string representing the date.

## Examples

``` r
pretty_date()
#> [1] "December 9, 2025"
```
