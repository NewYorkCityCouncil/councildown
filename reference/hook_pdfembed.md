# Embed fonts in PDF images

This knitr chunk hook embeds fonts in PDF images so that the appear
correctly on all computers.

## Usage

``` r
hook_pdfembed(before, options, envir)
```

## Arguments

- before:

  Is hook being run before chunk

- options:

  Chunk options

- envir:

  Environment
